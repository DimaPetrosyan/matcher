-- ─── Район и готовность добираться ────────────────────────────────────
-- Третий шаг онбординга. Точные координаты не запрашиваются и не хранятся:
-- район выбирается из списка, это минимизация ПДн на уровне интерфейса.

alter table app_user add column if not exists district      text;  -- код района из справочника в коде; null — шаг ещё не пройден
alter table app_user add column if not exists travel_radius text;  -- 'near' | 'thirty' | 'any'; насколько далеко человек готов ехать
